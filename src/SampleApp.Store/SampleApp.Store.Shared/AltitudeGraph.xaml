<UserControl x:Class="SampleApp.AltitudeGraph"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleApp"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<TextBlock x:Name="maxtb" VerticalAlignment="Top" FontSize="12" Margin="0,-6,0,0" />
		<TextBlock x:Name="mintb" VerticalAlignment="Bottom" FontSize="12" Margin="0,0,0,-6" />
		<Border BorderBrush="CornflowerBlue" BorderThickness="0,1"
				Grid.Column="1" Opacity=".5" />
		<Grid  Grid.Column="1">
			
			<Path Stretch="Fill"
				  StrokeLineJoin="Round"
				  Stroke="CornflowerBlue"
				  StrokeThickness="2"
				  x:Name="path"
				  />
		</Grid>
    </Grid>
</UserControl>
